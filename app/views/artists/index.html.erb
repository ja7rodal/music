<h1>Artistas?</h1>
<div class="">
  <% @artists.each do |artist| %>
  <div>

    <img src="<%= artist.image_url %>" alt="" />
  </div>
  <div class="">
      <%= artist.name %>
  </div>
  <% end %>
  <p>

  </p>
</div>
